# Python Requirements for PDF Navigation Prototype
# =================================================

# Core PDF processing
PyMuPDF==1.23.14
fitz==0.0.1.dev2

# GUI framework
tkinter  # Usually included with Python

# Image processing for PDF rendering
Pillow==10.1.0

# Optional: For better text processing
regex==2023.10.3

# Installation:
# pip install PyMuPDF Pillow regex

# Note: tkinter comes with Python by default on most systems

# Optional: HTTP client used for pushing JSON to Firebase from the exporter
requests==2.31.0

# Admin SDK for server-side Firebase writes (optional)
firebase-admin==6.1.0
